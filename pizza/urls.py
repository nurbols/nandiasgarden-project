from django.urls import path 
#from .views import HomePageView, OrderPageView

'''urlpatterns = [
    path('', HomePageView.as_view(), name='home'), 
    path('order/', OrderPageView.as_view(), name='order'), 
]  ''' 
